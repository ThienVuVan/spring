version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: %s
        LoadBalancerInfo:
          ContainerName: %s
          ContainerPort: %s
